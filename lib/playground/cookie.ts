/** Shared cookie name for the playground sandbox id. Kept dependency-free so it
 *  can be imported from both edge middleware and the server-only client. */
export const PLAYGROUND_COOKIE = "fb_pg";
